services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: insta_tracking
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
